export function backSubstitution(system) {
  console.log(system);
  //change to row reduced etchlon form
  // prerec make list of leading positions during past iteration
  // start with last leading position in list/current pivot
  // fix value
  // move to leading postion one before current in list
  // call this recursively
}
